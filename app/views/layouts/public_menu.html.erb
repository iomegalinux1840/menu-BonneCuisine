<!DOCTYPE html>
<html>
  <head>
    <title><%= @restaurant_branding[:name] %> - Menu</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>

    <% if @restaurant_branding[:font_family] == 'Playfair Display' %>
      <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&family=Source+Sans+Pro:wght@300;400;600&display=swap" rel="stylesheet">
    <% else %>
      <link href="https://fonts.googleapis.com/css2?family=<%= @restaurant_branding[:font_family].gsub(' ', '+') %>:wght@400;700&family=Source+Sans+Pro:wght@300;400;600&display=swap" rel="stylesheet">
    <% end %>

    <style>
      :root {
        --primary-color: <%= @restaurant_branding[:primary_color] %>;
        --secondary-color: <%= @restaurant_branding[:secondary_color] %>;
        --font-family-heading: '<%= @restaurant_branding[:font_family] %>', serif;
      }

      body {
        background-color: var(--secondary-color);
      }

      h1, h2, h3 {
        font-family: var(--font-family-heading);
        color: var(--primary-color);
      }

      .menu-header {
        background-color: var(--primary-color);
        color: white;
        padding: 2rem;
        text-align: center;
        box-shadow: 0 2px 8px rgba(0,0,0,0.1);
      }

      .menu-header h1 {
        color: white;
        margin: 0;
        font-size: 3rem;
      }

      .restaurant-logo {
        max-height: 80px;
        margin-bottom: 1rem;
      }

      .menu-item {
        background-color: white;
        border: 2px solid var(--primary-color);
      }

      .menu-item-name {
        color: var(--primary-color);
        font-family: var(--font-family-heading);
      }

      .menu-item-price {
        color: var(--primary-color);
        font-weight: bold;
      }
    </style>
  </head>

  <body>
    <div class="menu-header">
      <% if @restaurant_branding[:logo_url].present? %>
        <%= image_tag @restaurant_branding[:logo_url], class: "restaurant-logo", alt: @restaurant_branding[:name] %>
      <% end %>
      <h1><%= @restaurant_branding[:name] %></h1>
      <p class="tagline">Notre Menu</p>
    </div>

    <main class="container">
      <%= yield %>
    </main>

    <footer class="menu-footer">
      <p>&copy; <%= Date.current.year %> <%= @restaurant_branding[:name] %>. Tous droits réservés.</p>
    </footer>

    <%= turbo_stream_from "menu_channel_#{current_restaurant.id}" if current_restaurant %>
  </body>
</html>