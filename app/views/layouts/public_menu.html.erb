<!DOCTYPE html>
<html>
  <head>
    <title><%= @restaurant_branding[:name] %> - Menu</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <% if ENV['STRIPE_PUBLISHABLE_KEY'].present? %>
      <meta name="stripe-publishable-key" content="<%= ENV['STRIPE_PUBLISHABLE_KEY'] %>">
      <script src="https://js.stripe.com/v3/"></script>
      <% if ENV['STRIPE_PRICING_TABLE_ID'].present? %>
        <script async src="https://js.stripe.com/v3/pricing-table.js"></script>
      <% end %>
    <% end %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>

    <% if @restaurant_branding[:font_family] == 'Playfair Display' %>
      <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&family=Source+Sans+Pro:wght@300;400;600&display=swap" rel="stylesheet">
    <% else %>
      <link href="https://fonts.googleapis.com/css2?family=<%= @restaurant_branding[:font_family].gsub(' ', '+') %>:wght@400;700&family=Source+Sans+Pro:wght@300;400;600&display=swap" rel="stylesheet">
    <% end %>

    <style>
      :root {
        --primary-color: <%= @restaurant_branding[:primary_color] %>;
        --secondary-color: <%= @restaurant_branding[:secondary_color] %>;
        --font-family-heading: '<%= @restaurant_branding[:font_family] %>', serif;
      }

      body.public-menu {
        background: radial-gradient(circle at 10% 20%, rgba(255,255,255,0.15), transparent 45%),
                    linear-gradient(135deg, rgba(43, 87, 151, 0.95) 0%, rgba(74, 123, 200, 0.85) 48%, rgba(138, 182, 255, 0.78) 100%);
        min-height: 100vh;
      }

      h1, h2, h3 {
        font-family: var(--font-family-heading);
      }

      .public-menu .restaurant-nav {
        background: linear-gradient(120deg, rgba(15, 23, 42, 0.65), rgba(15, 23, 42, 0.35));
        backdrop-filter: blur(14px);
        border-bottom: 1px solid rgba(248, 250, 252, 0.18);
      }

      .public-menu .restaurant-nav h1 {
        font-family: var(--font-family-heading);
      }

      .public-menu .restaurant-container {
        position: relative;
      }

      .public-menu .restaurant-container::before {
        content: "";
        position: absolute;
        inset: 0;
        background: rgba(255,255,255,0.06);
        border-radius: 36px;
        filter: blur(0);
        pointer-events: none;
      }

      .public-menu .restaurant-header {
        border-radius: 28px;
        border: 1px solid rgba(248, 250, 252, 0.14);
        background: linear-gradient(140deg, rgba(255,255,255,0.18), rgba(255,255,255,0.07));
        backdrop-filter: blur(18px);
      }

      .public-menu .menu-item {
        background: linear-gradient(160deg, rgba(248, 250, 252, 0.85), rgba(255, 255, 255, 0.7));
        border: 1px solid rgba(248, 250, 252, 0.25);
        box-shadow: 0 18px 35px rgba(15, 23, 42, 0.18);
      }

      .public-menu .menu-item.has-image .menu-item-photo {
        box-shadow: 0 12px 24px rgba(15, 23, 42, 0.25);
      }

      .public-menu .menu-item-name,
      .public-menu .menu-item-price {
        color: #0f172a;
      }

      .public-menu .menu-item-price {
        background: rgba(15, 23, 42, 0.08);
        border-radius: 999px;
        padding: 0.25rem 0.9rem;
      }

      .public-menu .menu-message {
        background: linear-gradient(140deg, rgba(255,255,255,0.2), rgba(255,255,255,0.05));
        border: 1px solid rgba(248, 250, 252, 0.18);
        box-shadow: 0 20px 40px rgba(15, 23, 42, 0.25);
      }
    </style>
  </head>

  <body class="public-menu"
        data-restaurant-id="<%= current_restaurant&.id %>"
        data-menu-refresh-url="<%= "#{request.path}.json" %>">
    <!-- Blue Header Bar -->
    <nav class="restaurant-nav">
      <div class="nav-content">
        <h1><%= @restaurant_branding[:name] %></h1>
        <div class="nav-actions">
          <a href="<%= restaurant_admin_login_path(restaurant_slug: params[:restaurant_slug]) %>" class="admin-link">
            🔧 Administration
          </a>
        </div>
      </div>
    </nav>

    <!-- Main Content -->
    <div class="restaurant-container">
      <!-- Restaurant Branding Header -->
      <% if @restaurant_branding[:logo_url].present? %>
        <div class="restaurant-header">
          <div class="logo-container">
            <div class="logo-wrapper">
              <%= image_tag @restaurant_branding[:logo_url], class: "restaurant-logo", alt: @restaurant_branding[:name] %>
            </div>
          </div>
        </div>
      <% end %>

      <!-- Menu Content -->
      <main>
        <%= yield %>
      </main>

      <!-- Footer -->
      <footer class="restaurant-footer">
        <p>&copy; <%= Date.current.year %> <%= @restaurant_branding[:name] %>. Tous droits réservés.</p>
        <p style="font-size: 0.9rem; opacity: 0.8; margin-top: 0.5rem;">
          Propulsé par MenuPlatform
        </p>
      </footer>
    </div>

  </body>
</html>
