test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

production:
  service: Disk
  root: <%= Rails.root.join("storage") %>

# Alternative configuration for Railway
railway:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>