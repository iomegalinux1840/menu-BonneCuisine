test:
  service: Disk
  root: <%= Rails.root.join("data/storage/test") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

production:
  service: Disk
  root: <%= Rails.root.join("data/storage/production") %>

# Alternative configuration for Railway
railway:
  service: Disk
  root: <%= Rails.root.join("data/storage/production") %>
